#vert

void vert() {
	
}
#end

#frag

texture0 tex;

void frag() {
	vec4 temp = texture(tex, vec2(lesl.textureCoord.x, 1.0 - lesl.textureCoord.y));
    // lesl.outColor = temp;
    lesl.outColor = vec4(1.0 - temp.r, 1.0 - temp.g, 1.0 - temp.b, temp.a);
}
#end
